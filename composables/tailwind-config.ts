export function useTailwindConfig() {
  const defaultTransition = 'transition ease-in-out duration-350'
  const borderColor = 'border-white-200 dark:border-gray-700'

  return {
    defaultTransition,
    borderColor,
  }
}
